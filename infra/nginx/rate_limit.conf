# Nginx Rate Limiting Zones (Loaded inside http {} context via /etc/nginx/conf.d/)
limit_req_zone $binary_remote_addr zone=ip_limit:10m rate=30r/s;
limit_req_zone $binary_remote_addr zone=prod_api_limit:10m rate=50r/s;
